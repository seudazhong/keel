[project]
name = "keel-sandbox"
version = "0.0.0"
description = "Keel sandbox executor image and agent (stub; lands in M1)."
requires-python = ">=3.12"
dependencies = [
    "keel-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/keel_sandbox"]
